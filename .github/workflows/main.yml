on:
  push:
    branches:
      - master 



jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: "Login to docker hub"
        run: docker login --username mohamedshalash --password dckr_pat_le4G5fuWPnUHO4P1eeEAc-VMtEk